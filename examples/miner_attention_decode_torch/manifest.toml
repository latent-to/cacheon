# Graph-native MiniMax-M3 sparse-decode attention bundle. Stock code owns score and
# top-k selection; this entry attends only the validator-supplied paged block set.

bundle_id = "example-attention-decode-torch"
abi_version = "cacheon-op-abi-v0"

[[ops]]
slot = "attention.decode"
source = "kernels/attention_decode.py"
entry = "attention_decode"
dtypes = ["float32", "bfloat16", "float16"]
metadata = "metadata/attention_decode.json"
