# inference_engine — Python 3.10+
#
# Key version constraints:
#   torch               — GPU pods: installed by setup.sh from
#                         https://download.pytorch.org/whl/cu128 (matches CUDA 12.x drivers).
#                         Not pinned here so `pip install -r` alone won't pull a mismatched wheel.
#   transformers>=4.40  — Qwen2 attention forward gained `position_embeddings`
#                         as an explicit parameter in 4.40. Older versions
#                         compute RoPE internally and won't pass it to the
#                         patched forward, breaking the monkey-patch.
#   accelerate>=0.27    — required by from_pretrained(..., device_map=...)

transformers>=4.40.0
accelerate>=0.27.0
sentencepiece>=0.1.99
tokenizers>=0.15.0

datasets>=2.14.0
huggingface_hub==1.11.0

# dev / testing
pytest>=8.0.0