[pytest]
markers =
    unit: no model download, no GPU — runs anywhere (deselect with '-m "not unit"')
    integration: requires Qwen2.5-0.5B-Instruct (~1 GB download) and CPU/GPU
    arch: validates 7B GQA topology with random weights — no download, CPU only
testpaths = tests
